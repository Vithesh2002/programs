Include<studio.h>
Include<math.h>
void isprime(int n)
{
for (int i=1;i*i<=n;i++)
{
if ((n % i == 0) && (n / i == i))
{
return 1;
}
}
return 0;
}
void Main()
{
int n;
Printf("Enter the number");
Scanf("%d",&n);
if (isPerfect(n))
Printf("Yes");
else
Printf("No");
}
}
